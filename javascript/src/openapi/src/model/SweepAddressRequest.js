/**
 * WaaS OpenAPI Documentation
 * This is a WaaS OpenAPI Server.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SweepAddressRequest model module.
 * @module model/SweepAddressRequest
 * @version 1.0
 */
class SweepAddressRequest {
    /**
     * Constructs a new <code>SweepAddressRequest</code>.
     * @alias module:model/SweepAddressRequest
     * @param feeAddress {String} address to pay the fee
     * @param toAddress {String} address to sweep to
     * @param tokenId {String} token id to sweep
     */
    constructor(feeAddress, toAddress, tokenId) { 
        
        SweepAddressRequest.initialize(this, feeAddress, toAddress, tokenId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, feeAddress, toAddress, tokenId) { 
        obj['fee_address'] = feeAddress;
        obj['to_address'] = toAddress;
        obj['token_id'] = tokenId;
    }

    /**
     * Constructs a <code>SweepAddressRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SweepAddressRequest} obj Optional instance to populate.
     * @return {module:model/SweepAddressRequest} The populated <code>SweepAddressRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SweepAddressRequest();

            if (data.hasOwnProperty('fee_address')) {
                obj['fee_address'] = ApiClient.convertToType(data['fee_address'], 'String');
            }
            if (data.hasOwnProperty('max_fee_limit')) {
                obj['max_fee_limit'] = ApiClient.convertToType(data['max_fee_limit'], 'String');
            }
            if (data.hasOwnProperty('min_collect_amount')) {
                obj['min_collect_amount'] = ApiClient.convertToType(data['min_collect_amount'], 'String');
            }
            if (data.hasOwnProperty('to_address')) {
                obj['to_address'] = ApiClient.convertToType(data['to_address'], 'String');
            }
            if (data.hasOwnProperty('token_id')) {
                obj['token_id'] = ApiClient.convertToType(data['token_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SweepAddressRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SweepAddressRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SweepAddressRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['fee_address'] && !(typeof data['fee_address'] === 'string' || data['fee_address'] instanceof String)) {
            throw new Error("Expected the field `fee_address` to be a primitive type in the JSON string but got " + data['fee_address']);
        }
        // ensure the json data is a string
        if (data['max_fee_limit'] && !(typeof data['max_fee_limit'] === 'string' || data['max_fee_limit'] instanceof String)) {
            throw new Error("Expected the field `max_fee_limit` to be a primitive type in the JSON string but got " + data['max_fee_limit']);
        }
        // ensure the json data is a string
        if (data['min_collect_amount'] && !(typeof data['min_collect_amount'] === 'string' || data['min_collect_amount'] instanceof String)) {
            throw new Error("Expected the field `min_collect_amount` to be a primitive type in the JSON string but got " + data['min_collect_amount']);
        }
        // ensure the json data is a string
        if (data['to_address'] && !(typeof data['to_address'] === 'string' || data['to_address'] instanceof String)) {
            throw new Error("Expected the field `to_address` to be a primitive type in the JSON string but got " + data['to_address']);
        }
        // ensure the json data is a string
        if (data['token_id'] && !(typeof data['token_id'] === 'string' || data['token_id'] instanceof String)) {
            throw new Error("Expected the field `token_id` to be a primitive type in the JSON string but got " + data['token_id']);
        }

        return true;
    }


}

SweepAddressRequest.RequiredProperties = ["fee_address", "to_address", "token_id"];

/**
 * address to pay the fee
 * @member {String} fee_address
 */
SweepAddressRequest.prototype['fee_address'] = undefined;

/**
 * max fee limit
 * @member {String} max_fee_limit
 */
SweepAddressRequest.prototype['max_fee_limit'] = undefined;

/**
 * min collect amount
 * @member {String} min_collect_amount
 */
SweepAddressRequest.prototype['min_collect_amount'] = undefined;

/**
 * address to sweep to
 * @member {String} to_address
 */
SweepAddressRequest.prototype['to_address'] = undefined;

/**
 * token id to sweep
 * @member {String} token_id
 */
SweepAddressRequest.prototype['token_id'] = undefined;






export default SweepAddressRequest;

